Użytkownik:
/lol join <arena>

  Permission:
  		lol.*
  		lol.user.*
  		lol.user.join.*
  		lol.user.join.<arena>
  
/lol leave

	Permission:
  		lol.*
		lol.user.*
		lol.user.leave
		
/lol spectate <arena>

	opis:
		Jest nie widoczny dla grających na arenie, może latać i patrzeć.
	Permission:
  		lol.*
  		lol.user.*
		lol.user.spectate.*
		lol.user.spectate.<arena>

/lol stat [user]
     
	Permission:
		lol.*
		lol.user.*
		lol.user.stat
		lol.user.stat.other

/lol stat reset

	Permission:
		lol.*
		lol.user.*
		lol.user.stat.*
		lol.user.stat.reset
	

/lol building 

    opis: Budowanie na terenie aren
    
    Permission:
    lol.*
    lol.admin.building
    

 
